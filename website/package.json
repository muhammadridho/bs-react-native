{
  "private": true,
  "name": "bs-react-native-website",
  "version": "0.0.0",
  "scripts": {
    "clean:dist": "trash dist",
    "clean:build": "trash build",
    "clean": "yarn clean:build && yarn clean:dist",
    "build:odoc-html": "./scripts/odoc-to-html.sh",
    "build:intermediate": "node scripts/OdocHtmlToIntermediate.bs.js",
    "build:html": "yarn clean:dist && node scripts/IntermediateToHtml.entry.js",
    "build": "yarn build:odoc-html && yarn build:intermediate && yarn build:html",
    "test": "yarn build"
  },
  "devDependencies": {
    "bs-platform": "^5.0.0",
    "glob": "^7.1.3",
    "mkdirp": "^0.5.1",
    "module-alias": "^2.2.0",
    "react-native-web": "^0.11.2",
    "reason-future": "^2.4.0",
    "reason-react": "https://github.com/reasonml/reason-react.git#0b4140c",
    "trash-cli": "^1.4.0"
  }
}
